# Yealink remote phonebook config - merge into the phone's main
# provisioning file (see voip-phone-provisioning-guides).

remote_phonebook.data.1.url = https://provisioning.example.com/phonebook/acme.xml
remote_phonebook.data.1.name = Company Directory
remote_phonebook.flash_time = 21600   ; re-fetch every 6 hours
